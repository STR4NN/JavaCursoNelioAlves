package org.example.Collections;

public class CollectionsConcept {
    // Não é preciso definir tamanho de uma Collection
    // Deixam você mesclar diferentes tipos em uma só collection
    // Não aceitam tipos primitivos (int) apenas Wrappers (Integer)


    // PRINCIPAIS

    // SET = Conjunto, não ORDENADO, não INDEXADO, e não aceita repetição. Estrutura de DADOS. add.

    // LIST = Lista, indexado ( pode acessar valores pelo indice ), aceita repetição. Ordenada. add.

    // Map = Chave e valor, chave não aceita repetição, valor aceita repetição. (duas colunas)

    // Queue = Fila, implementa FIFA, primeiro a entrar é o primeiro a sair. FIFO = first in/ first out;

    // Stack = Pilha, Ultimo a entrar é o primeiro a sair;

    // Possuem funções comuns a todas a Collections


}
